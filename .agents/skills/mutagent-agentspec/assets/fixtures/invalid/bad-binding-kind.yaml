# INVALID — operator ruling R3 (2026-07-22): binding kind is a CLOSED enum {cli|saas|mcp|sdk|host-tool}.
# A context access `kind` outside that set must FAIL validation (fail-loud, F02 closed core).
apiVersion: agentspec.mutagent.io/v0.3.0
kind: Agent
metadata: { id: x, name: X, version: "1.0.0", description: "x" }
spec:
  intent:
    problem: "p"
    outcomes: []
    sop: []
    jobs: []
    constraints: []
    nonGoals: []
    assumptions: []
    unknowns: []
  context:
    - id: c1
      description: "a context read with an unlisted binding kind"
      modalities: [record]
      source: somewhere
      access: { kind: carrier-pigeon, ref: r, allowedOperations: [read] }   # <-- kind not in the closed enum
  actions: []
  capabilities: { code: [], skills: [], delegates: [] }
  agent:
    persona: { role: r, description: d }
    systemPrompt: "sp"
    operatingType: automation
  targets: []
  evaluation: { criteria: [], scenarios: [], datasets: [] }
