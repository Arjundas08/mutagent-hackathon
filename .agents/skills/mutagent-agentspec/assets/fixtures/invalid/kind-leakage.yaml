# INVALID — exit check: "kind leakage fails". kind is Skill but the card carries an `agent` body
# (and no `skill` body): the semantic validator must reject the leaked body AND the missing one.
apiVersion: agentspec.mutagent.io/v0.3.0
kind: Skill
metadata: { id: x, name: X, version: "1.0.0", description: "x" }
spec:
  intent:
    problem: "p"
    outcomes: []
    sop: []
    jobs: []
    constraints: []
    nonGoals: []
    assumptions: []
    unknowns: []
  context: []
  actions: []
  capabilities: { code: [], skills: [], delegates: [] }
  agent:                                              # <-- leaked Agent body on a Skill card
    persona: { role: r, description: d }
    systemPrompt: "sp"
    operatingType: automation
  targets: []
  evaluation: { criteria: [], scenarios: [], datasets: [] }
